class pat05 {
    public static void main(String[] args) {
        Scanner scan new Scanner(System.in);
        String sampleNumber = scan.nextLine();
        int n = Integer.parseInt(sampleNumber);
        String sample = scan.nextLine();
    }
}